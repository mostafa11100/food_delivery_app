import 'package:flutter/material.dart';
import '../contest/colorapp.dart';

ThemeData themenglish = ThemeData(
    fontFamily: 'RobotoMono',
    textTheme: TextTheme(
      titleSmall: TextStyle(color: colorapp.textcolor, fontSize: 20),
      bodySmall: TextStyle(color: colorapp.textcolor, fontSize: 12),
    ));

ThemeData themearpic = ThemeData(
    fontFamily: 'Cairo',
    textTheme: TextTheme(
      titleSmall: TextStyle(color: colorapp.textcolor, fontSize: 16),
      bodySmall: TextStyle(color: colorapp.textcolor, fontSize: 8),
    ));
